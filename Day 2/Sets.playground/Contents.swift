import UIKit

// Sets are like arrays, but there is no order and no item can appear twice

let colors = Set(["red", "green", "blue"])
let colors2 = Set(["red", "green", "blue", "red"]) // duplicates only show once
