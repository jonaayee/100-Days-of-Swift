import UIKit

let firstScore = 12
let secondScore = 10

let sum = firstScore + secondScore // Adds
let difference = firstScore - secondScore // Subtracts

let product = firstScore * secondScore // Multiplies
let quotient = firstScore / secondScore // Divides

let remainder = firstScore % secondScore // MOD divides the value and returns the remaining value that does not go in evenly

let isEven = firstScore.isMultiple(of: 2) // You can use the modifier ".isMultiple(of: _)" for a similar result


