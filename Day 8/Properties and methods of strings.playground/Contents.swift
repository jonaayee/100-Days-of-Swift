import UIKit

let string = "Do or do not, there is no try." // Strings are a struct, they have their own properties

print(string.count) // counts the characters
print(string.hasPrefix("Do")) // checks for prefix
print(string.uppercased()) // makes all characters uppercase
print(string.lowercased()) // makes all characters lowercase
print(string.sorted()) // sorts characters into an array

