import UIKit

// Checks the first value, if true then returns the second value, if false returns the third value

let firstCard = 11
let secondCard = 10

print(firstCard == secondCard ? "Cards are the same" : "Cards are different")

// Same code via if-else statements
if firstCard == secondCard {
    print("Cards are the same")
} else {
    print("Cards are different")
}
